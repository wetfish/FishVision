apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

namespace: factory-staging

patches:
  # otlp-patch.yaml patches factory-web/factory-worker which live in the factory repo.
  # Apply it there: kubectl patch -k <factory-repo>/k8s/overlays/staging/ --patch-file otlp-patch.yaml
  - path: promtail-env-patch.yaml
